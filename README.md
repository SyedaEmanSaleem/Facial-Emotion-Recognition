# Facial-Emotion-Recognition
This project implements a Facial Emotion Recognition (FER) system built using a Custom Convolutional Neural Network (CNN). The goal of the model is to automatically classify human facial expressions into predefined emotion categories using grayscale face images.
